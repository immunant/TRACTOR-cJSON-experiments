Testing out running `git init` within the sandbox to give the agent a git repo
to work with.

Second run includes tweaks to the `cargo check-unsafe2` command to verify that
temp target dir is working as expected.
